interface DateParts {
    month: string;
    day: string;
    year: string;
}
/**
 * Determines the field order for date spinbuttons based on locale.
 */
declare function getFieldOrder(locale: string): Array<'month' | 'day' | 'year'>;
/**
 * Gets the date separator character for a locale.
 */
declare function getDateSeparator(locale: string): string;
/**
 * Parses an ISO date string into month, day, year parts.
 */
declare function parseISODate(isoDate: string): DateParts | null;
/**
 * Builds an ISO date string from month, day, year parts.
 */
declare function buildISODate(month: string, day: string, year: string): string;
/**
 * Gets the placeholder text for a date field based on locale order.
 */
declare function getPlaceholder(locale: string): string;
/**
 * Gets the max value for a spinbutton field.
 */
declare function getFieldMax(field: 'month' | 'day' | 'year', month?: number, year?: number): number;
/**
 * Gets the min value for a spinbutton field.
 */
declare function getFieldMin(field: 'month' | 'day' | 'year'): number;
export { buildISODate, getDateSeparator, getFieldMax, getFieldMin, getFieldOrder, getPlaceholder, parseISODate };
export type { DateParts };

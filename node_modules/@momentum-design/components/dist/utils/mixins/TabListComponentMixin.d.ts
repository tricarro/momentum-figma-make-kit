import type { Component } from '../../models';
import Tab from '../../components/tab';
import type { BaseArray } from '../virtualIndexArray';
import type { Constructor } from './index.types';
export declare abstract class TabListComponentMixinInterface {
    protected abstract get navItems(): BaseArray<Tab>;
    activeTabId?: string;
}
export declare const TabListComponentMixin: <T extends Constructor<Component>>(superClass: T) => Constructor<TabListComponentMixinInterface> & T;

# Cursor

Repo-local Cursor assets:

- **[`rules/`](rules/README.md)** — Momentum `.mdc` project rules (generated from `figme-make/guidelines/`), build script, and helper to copy rules into `.cursor/rules/`.
- **`skills/`** — Drop Cursor skill files here when you add team-specific skills for this kit.

import { getWeekStartDay } from '../../utils/date-utils';
import type { CalendarGridWeek } from './calendar.types';
/**
 * Returns abbreviated weekday names for the given locale, ordered by locale week start.
 * E.g. for 'en-US': ['S', 'M', 'T', 'W', 'T', 'F', 'S']
 */
declare function getWeekdayNames(locale: string): string[];
/**
 * Generates the 6-row calendar grid for a given month/year.
 */
declare function generateCalendarGrid(year: number, month: number, locale: string, selectedDates: string[], rangeStart: string | undefined, rangeEnd: string | undefined, minDate: string | undefined, maxDate: string | undefined): CalendarGridWeek[];
/**
 * Returns the ISO dates for the week containing the given date.
 */
declare function getWeekRange(dateIso: string, locale: string): {
    start: string;
    end: string;
};
/**
 * Returns the localized month and year string.
 */
declare function getMonthYearLabel(year: number, month: number, locale: string): string;
/**
 * Returns all ISO date strings between start and end (inclusive).
 */
declare function getDatesInRange(startIso: string, endIso: string): string[];
/**
 * Format a date for display.
 */
declare function formatDateForDisplay(dateIso: string, locale: string): string;
/**
 * Format a date range for display.
 */
declare function formatDateRangeForDisplay(startIso: string, endIso: string, locale: string): string;
export { formatDateForDisplay, formatDateRangeForDisplay, generateCalendarGrid, getDatesInRange, getMonthYearLabel, getWeekdayNames, getWeekRange, getWeekStartDay, };

/**
 * Lightweight date utilities using native Date and Intl APIs.
 * Replaces Luxon for calendar/datepicker components.
 *
 * All dates are handled as local dates (no timezone conversion).
 * ISO strings are in yyyy-mm-dd format.
 */
declare function toISODate(d: Date): string;
declare function parseISO(iso: string): Date | null;
declare function createDate(year: number, month: number, day?: number): Date;
declare function today(): Date;
declare function addDays(d: Date, days: number): Date;
/**
 * Add months, clamping the day to the last day of the target month
 * to match Luxon behavior (e.g. March 31 - 1 month → Feb 28, not March 3).
 */
declare function addMonths(d: Date, months: number): Date;
/**
 * Add years, clamping the day to the last day of the target month
 * (handles Feb 29 in leap years → Feb 28 in non-leap years).
 */
declare function addYears(d: Date, years: number): Date;
declare function isSameDay(a: Date, b: Date): boolean;
declare function isBefore(a: Date, b: Date): boolean;
declare function isAfter(a: Date, b: Date): boolean;
declare function isBeforeOrEqual(a: Date, b: Date): boolean;
declare function startOfMonth(d: Date): Date;
declare function endOfMonth(d: Date): Date;
declare function daysInMonth(year: number, month: number): number;
/**
 * Returns the ISO weekday: 1=Monday .. 7=Sunday
 */
declare function isoWeekday(d: Date): number;
declare function startOfWeek(d: Date, weekStartDay?: number): Date;
declare function endOfWeek(d: Date, weekStartDay?: number): Date;
/**
 * Locale-aware weekday names using Intl.DateTimeFormat.
 */
declare function getWeekdayNames(locale: string, style?: 'narrow' | 'short' | 'long'): string[];
/**
 * Returns the starting day of the week for a locale.
 * 1 = Monday (ISO), 7 = Sunday.
 */
declare function getWeekStartDay(locale: string): number;
/**
 * Locale-aware month + year label (e.g. "July 2025").
 */
declare function getMonthYearLabel(year: number, month: number, locale: string): string;
/**
 * Locale-aware full date label for aria (e.g. "July 15, 2025").
 */
declare function formatDateFull(iso: string, locale: string): string;
/**
 * Locale-aware medium date format (e.g. "Jul 15, 2025").
 */
declare function formatDateMed(iso: string, locale: string): string;
/**
 * Format a date range for display using the native Intl.DateTimeFormat.formatRange API.
 * Produces locale-correct output for all cases: same month, cross-month, and cross-year ranges.
 * e.g. en-US: "July 13 – 19, 2025", de-DE: "13.–19. Juli 2025", ja-JP: "2025年7月13日～19日"
 */
declare function formatDateRange(startIso: string, endIso: string, locale: string): string;
/**
 * Get the locale-specific date parts order and separator.
 */
declare function getLocaleDateParts(locale: string): {
    order: Array<'month' | 'day' | 'year'>;
    separator: string;
};
export { addDays, addMonths, addYears, createDate, daysInMonth, endOfMonth, endOfWeek, formatDateFull, formatDateMed, formatDateRange, getLocaleDateParts, getMonthYearLabel, getWeekdayNames, getWeekStartDay, isAfter, isBefore, isBeforeOrEqual, isSameDay, isoWeekday, parseISO, startOfMonth, startOfWeek, today, toISODate, };
